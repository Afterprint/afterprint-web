'use client';
import Link from 'next/link';
import {useState} from 'react';
import {api} from '@/lib/api';
import {ArrowUpRight,Fingerprint} from 'lucide-react';
export default function Login(){const [error,setError]=useState('');const [busy,setBusy]=useState(false);return <main className="login-page"><section className="login-art"><Link href="/" className="brand">afterprint</Link><div><Fingerprint size={65} strokeWidth={.7}/><h1>Truth leaves<br/><span>a trace.</span></h1><p>Your evidence. Its context. One secure workspace.</p></div><p>Evidence intelligence. Human judgment.</p></section><section className="login-form"><div className="section-label">YOUR SECURE WORKSPACE</div><h2>Welcome back.</h2><p>Sign in with your organization’s account.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{await api('/auth/login',{method:'POST',body:JSON.stringify(Object.fromEntries(f))});window.location.href='/cases'}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}><label className="form-field">Email<input name="email" type="email" autoComplete="username" required/></label><label className="form-field">Password<input name="password" type="password" autoComplete="current-password" required minLength={12}/></label>{error&&<div className="error-box" role="alert">{error}</div>}<button disabled={busy} className="button orange">{busy?'Signing in…':'Sign in'}<ArrowUpRight size={17}/></button></form><p><Link href="/cases/demo">Explore the synthetic sample case →</Link></p></section></main>}
